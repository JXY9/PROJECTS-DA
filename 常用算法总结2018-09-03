1，k近邻（K-NN）
1）分类算法，也可应用于回归。无参数学习（对底层数据分布不做假设），基于实例（没有显式学习模型），所以也叫惰性算法。
2）如何做出预测（分类）？
目标是未标记对象（某）-计算与标记对象之间的距离-确定k-取数量最多的最近邻点的类标签作此对象的类标签（三要素）
（1）k  大：噪声影响大
       小：计算成本高
       # 遍历每个可能的k，按需选择——可能是好的选择。
       # 输出的是一个类别。 
（2）距离：      
（3）标记样本：
